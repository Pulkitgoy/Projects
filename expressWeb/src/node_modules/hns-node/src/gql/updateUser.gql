mutation updateUser($refId: ID!, $data: UpdateUserData!) {
  updateUser(refId: $refId, data: $data) {
    ...UserFragment
    app {
      ...AppFragment
    }
    channels {
      ...UserChannelFragment
    }
  }
}
