mutation saveUser($id: ID, $refId: ID, $data: SaveUserData!) {
  saveUser(id: $id, refId: $refId, data: $data) {
    ...UserFragment
    app {
      ...AppFragment
    }
    channels {
      ...UserChannelFragment
    }
  }
}
